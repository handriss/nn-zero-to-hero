services:
  jupyter:
    build: .
    container_name: zero-to-hero
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    environment:
      JUPYTER_TOKEN: ${JUPYTER_TOKEN:-changeme}
    restart: unless-stopped
